/*
Consultas que podem ser feitas em tabelas do próprio PostgreSQL
*/

-- Retorna todas as tabelas que estão no SGBD 
SELECT * FROM INFORMATION_SCHEMA.TABLES
-- Retorna todas as colunas que estão no SGBD
SELECT * FROM INFORMATION_SCHEMA.COLUMNS
-- Retorna todas as rotinas que estão no SGBD
SELECT * FROM INFORMATION_SCHEMA.ROUTINES	
